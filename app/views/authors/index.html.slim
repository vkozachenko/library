- provide(:title, 'All users')
h1 All authors
p = link_to 'Add author', new_author_path

= will_paginate

ul.users
  - @authors.each do |author|
      li = link_to author.name, author

= will_paginate